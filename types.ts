export type NavigationLinkType = {
  name: string;
  Icon: any;
  href: string;
};

export type ProjectType = {
  id: string;
  name: string;
  shortDescription: string;
  githubUrl?: string;
  thumbnail: string;
};
